type: object
description: Хронология событий бронирования (включая продления, перемещения, обновления).
required:
  - reservationId
  - items
properties:
  reservationId:
    type: string
    format: uuid
  items:
    type: array
    items:
      $ref: './TimelineEntry.yaml'
